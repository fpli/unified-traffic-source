ALTER TABLE p_soj_cl_t.notification_sends DROP IF EXISTS PARTITION (event_dt >= '2024-08-12', event_dt <= '2024-08-13');

INSERT INTO p_soj_cl_t.notification_sends
(
    global_notif_id,
    notification_id,
    tracking_id,
    event_type,
    app_name,
    app_version,
    guid,
    user_name,
    user_id,
    page_id,
    app_id,
    site_id,
    mts_tracking_rowkey,
    platform_notfn_id,
    item_id,
    msg_content_id,
    saved_search_id,
    event_dt,
    event_timestamp,
    exprmnt_id,
    trtmnt_id,
    feature_id,
    campaign_id,
    interest_ids,
    espresso_id,
    is_uep,
    canvas_id,
    message_id,
    message_list,
    updated_ts
  )
SELECT
  global_tracking_id,
  notification_id,
  tracking_id,
  event_type,
  app_name,
  app_version,
  guid,
  user_name,
  user_id
  page_id,
  app_id,
  site_id,
  mts_trckng_rowkey,
  platform_notification_id,
  item_id,
  msg_content_id,
  saved_search_id,
  event_dt,
  event_timestamp,
  exprmnt_id,
  trtmnt_id,
  feature_id,
  campaign_id,
  interest_ids,
  espresso_id,
  is_uep,
  canvas_id,
  message_id,
  message_list,
  CURRENT_TIMESTAMP
FROM
  p_soj_cl_t.notsend_temp1;

